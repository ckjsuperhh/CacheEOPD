# =============================================================================
# requirements-cuda.txt - CUDA GPU 环境专用依赖
# =============================================================================
# 本文件仅包含 CUDA GPU 环境下需要的额外依赖。
# 安装方式：pip install -r requirements-cuda.txt（在已安装基础依赖之后）

flash-attn  # Flash Attention —— GPU 高效注意力计算内核，大幅提升 LLM 训练/推理速度