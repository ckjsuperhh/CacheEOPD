# FAPO Ray 运行时环境配置
#
# 定义 Ray 集群的运行时环境变量：
# - TORCH_NCCL_AVOID_RECORD_STREAMS: 避免 NCCL 记录流，减少 GPU 内存占用
# - VLLM_USE_V1: 启用 vLLM v1 引擎

working_dir: ./
excludes: ["/.git/"]
env_vars:
  TORCH_NCCL_AVOID_RECORD_STREAMS: "1"
  VLLM_USE_V1: "1"
